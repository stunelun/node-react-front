import { Link } from "react-router-dom";
import styled from "styled-components";

/**
 * not yet. Choose Route or link, 조사중...
 */
const LinkTest = styled(Link)`
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
`

export default LinkTest;