// /** Container 아직 미완성 */

// import React, { Component } from 'react';
// import { connect } from 'react-redux';
// import { ChangeCurrency } from '../Components/store/modules/changeCurrencyPair';

// class CurrencypairContainer extends Component {
//   handleSelect = (currencypair:string) => {
//     const { ChangeCurrency } = this.props;
//     console.log('what');
//     ChangeCurrency(currencypair);
//   };

//   render() {
//     const { viewCurrencyPair } = this.props;
//     return <Palette onSelect={this.handleSelect} selected={color} />;
//   }
// }

// // props 로 넣어줄 스토어 상태값
// const mapStateToProps = state => ({
//     viewCurrencyPair: state.viewCurrencyPair,
// });

// // props 로 넣어줄 액션 생성함수
// const mapDispatchToProps = dispatch => ({
//     ChangeCurrency: currencypair => dispatch(ChangeCurrency(currencypair)),
// });

// // 컴포넌트에 리덕스 스토어를 연동해줄 때에는 connect 함수 사용
// export default connect(
//   mapStateToProps,
//   mapDispatchToProps
// )(PaletteContainer);